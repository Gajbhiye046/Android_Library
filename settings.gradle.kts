pluginManagement {
    repositories {
        google {
            content {
                includeGroupByRegex("com\\.android.*")
                includeGroupByRegex("com\\.google.*")
                includeGroupByRegex("androidx.*")
            }
        }
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "helloWorld"
include(":app", ":flashlight")
project(":flashlight").projectDir = File("C:\\Users\\ag928515.ttl\\AndroidStudioProjects\\How_to_convert_project_to_library\\flashlight\\app")

 